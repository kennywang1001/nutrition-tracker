# production 用的環境變數範本——只有佔位符，沒有真的密鑰。
#
# 用法：複製這個檔案，改個名字（例如 .env.production），填入真的值，
# 再用 --env-file 指給 docker compose（docker compose 只會自動讀取
# 名字剛好叫 .env 的檔案；.env.production 這種名字要明確指定）：
#
#   cp .env.production.example .env.production
#   # 編輯 .env.production，填入下面兩個值
#   docker compose --env-file .env.production \
#     -f docker-compose.yml -f docker-compose.prod.yml up -d
#
# 絕對不要把填好真的值的檔案改名成 .env（會跟本機開發/測試用的 .env
# 衝突），也絕對不要把它加進版控。

# JWT 簽章密鑰。用 `openssl rand -hex 32` 產生，不要跟開發或其他環境共用。
# 換掉這個值可以強制登出所有現有的 session（見部署手冊的緊急處置章節）。
JWT_SECRET=

# 發佈埠要綁定的位址。填 NAS 的 Tailscale 位址（用 `tailscale ip -4` 查），
# 不要填 0.0.0.0——那會讓區域網路上的任何裝置都連得到，
# 讓「Tailscale 是邊界」這個前提形同虛設。
BIND_ADDR=
